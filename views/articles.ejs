<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Velora by HK - Journal Article</title>
  <style>
     /* Global Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
a{
  text-decoration: none;
}
    

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #0d0d0d; /* deep dark */
      margin: 0;
      padding: 0;
      line-height: 1.6;
      color: #ddd;
    }
header {
  background: linear-gradient(90deg, #111, #1a1a1a);
  padding: 15px 20px;
  border-bottom: 1px solid #222;
  box-shadow: 0 3px 10px rgba(0,0,0,0.6);
  position: sticky;
  top: 0;
  z-index: 1000;
}

.nav-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap; /* allow wrapping on small screens */
}

.logo {
  color: #f4b400;
  font-size: 1.6rem;
  font-weight: bold;
  letter-spacing: 1px;
  text-transform: uppercase;
  margin: 0;
  transition: color 0.3s ease;
}

.logo:hover {
  color: #fff;
}

nav .home-btn {
  background: #f4b400;
  color: #111;
  padding: 8px 14px;
  border-radius: 6px;
  text-decoration: none;
  font-weight: bold;
  transition: background 0.3s ease, transform 0.2s ease;
  font-size: 0.95rem;
}

nav .home-btn:hover {
  background: #d99a00;
  transform: scale(1.05);
}

/* Responsive Fix for Small Screens */
@media (max-width: 450px) {
  .nav-container {
    flex-direction: column;
    text-align: center;
    gap: 10px;
  }

  .logo {
    font-size: 1.3rem;
  }

  nav .home-btn {
    width: 100%;
    padding: 10px;
    font-size: 1rem;
  }
}


    .article-container {
      max-width: 900px;
      margin: 50px auto;
      background: #1a1a1a;
      box-shadow: 0 8px 25px rgba(0,0,0,0.5);
      border-radius: 15px;
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .article-container:hover {
      transform: translateY(-6px);
      box-shadow: 0 12px 30px rgba(0,0,0,0.8);
    }

    .article-header img {
      width: 100%;
      height: 450px;
      object-fit: cover;
      filter: brightness(0.85) contrast(1.1);
      transition: filter 0.4s ease;
    }

    .article-header img:hover {
      filter: brightness(1) contrast(1.2);
    }

    .article-content {
      padding: 30px;
    }

    .article-title {
      font-size: 2.2rem;
      font-weight: bold;
      margin-bottom: 15px;
      color: #f4b400; /* gold highlight */
      text-shadow: 0 2px 8px rgba(0,0,0,0.7);
      border-left: 5px solid #f4b400;
      padding-left: 15px;
    }

    .article-desc {
      font-size: 1.1rem;
      color: #bbb;
      margin-bottom: 25px;
      font-style: italic;
    }

    .article-body {
      font-size: 1.05rem;
      color: #ddd;
      line-height: 1.9;
    }

    .article-body p {
      margin-bottom: 18px;
    }

    .btn {
      display: inline-block;
      background: #f4b400;
      color: #111;
      padding: 10px 18px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      margin-top: 25px;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .btn:hover {
      background: #d99a00;
      transform: scale(1.05);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .article-container {
        margin: 20px;
      }
      .article-header img {
        height: 280px;
      }
      .article-title {
        font-size: 1.6rem;
      }
      .article-desc {
        font-size: 1rem;
      }
    }
  </style>
</head>
<header>
  <div class="nav-container">
    <a href="/"><h1 class="logo">Velora by HK</h1></a>
    <nav>
      <a href="/" class="home-btn">🏠 Home</a>
    </nav>
  </div>
</header>
<body>
<div class="article-container">
 
    <div class="article-header">
      <img src="/images/productimg/<%= articles.image %>" alt="Luxury Perfume Bottle" />
    </div>
    <div class="article-content">
      <h1 class="article-title"><%= articles.header %></h1>
      <p class="article-desc">
        <%= articles.shortdescription %>
      </p>

      <div class="article-body">
        <%= articles.longdescription %>
        </div>

      <a href="/" class="btn">← Back to Articles</a>
    </div>

</div>

</body>
</html>
