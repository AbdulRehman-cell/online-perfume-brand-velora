<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Velora Éclat Noir - Product Details</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
a{
  text-decoration:none ;
}
    body {
      font-family: Arial, sans-serif;
      background-color: #111;
      color: #eee;
      margin: 0;
      padding: 0;
    }
    :root {
  --bg: #0c0c0c;
  --bg-card: #161616;
  --text: #f9f9f9;
  --muted: #b5b5b5;
  --primary: #d4af37; /* luxury gold */
  --font-serif: "Playfair Display", serif;
  --font-sans: "Poppins", sans-serif;
}
    /* ---------------- HEADER ---------------- */
header {
  display: flex;
justify-content: space-between;
  padding: 22px 50px;
  position: sticky;
  top: 0;
  z-index: 999;
  background: rgba(12, 12, 12, 0.9);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
   margin: auto;
   

}

.logo .title {
  font-family: var(--font-serif);
  font-size: 30px;
  font-weight: 700;
  color: var(--primary);
}

.logo .subtitle {
  font-size: 13px;
  color: var(--muted);
  margin-top: -3px;
}

nav {
  display: flex;
  align-items: center;
  gap: 30px;
  max-width: 1300px;
}

nav a {
  text-decoration: none;
  font-size: 15px;
  font-weight: 500;
  color: var(--text);
  transition: color 0.3s ease;
}

nav a:hover {
  color: var(--primary);
}

.cta {
  background: var(--primary);
  color: #000;
  padding: 10px 26px;
  border-radius: 40px;
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
}

.cta:hover {
  background: #e0c04c;
}

/* Hamburger menu */
.hamburger {
  display: none;
  flex-direction: column;
  gap: 6px;
  cursor: pointer;
}

.hamburger span {
  height: 2px;
  width: 24px;
  background: var(--text);
}

/* ---------------- MOBILE NAV ---------------- */
.mobile-nav {
  position: fixed;
  top: 0;
  right: -100%;
  height: 100vh;
  width: 75%;
  background: var(--bg-card);
  display: none;
  flex-direction: column;
  padding: 40px;
  gap: 20px;
  transition: right 0.4s ease;
}

.mobile-nav.active {
  right: 0;
  text-align: center;
}

.mobile-nav a {
  color: var(--text);
  text-decoration: none;
  font-size: 18px;
}

.close-btn {
  align-self: flex-end;
  font-size: 26px;
  color: var(--text);
  border: none;
  background: none;
  cursor: pointer;
}


    .container {
      max-width: 1000px;
      margin: auto;
      padding: 20px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 30px;
      background: #1a1a1a;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
      margin-top: 40px;
    }

    .product-img img {
      width: 100%;
      border-radius: 15px;
    }

    .product-info h1 {
      font-size: 28px;
      margin-bottom: 10px;
      color: #fff;
    }

    .product-info p {
      margin-bottom: 15px;
      line-height: 1.6;
      color: #ccc;
    }

    .price {
      font-size: 22px;
      font-weight: bold;
      color: #f4b400;
      margin: 20px 0;
    }

    .order-form {
      background: #222;
      padding: 20px;
      border-radius: 10px;
      margin-top: 20px;
    }

    .order-form h2 {
      margin-bottom: 15px;
      font-size: 20px;
      color: #fff;
    }

    .order-form input, 
    .order-form select {
      width: 95%;
      padding: 12px;
      margin-bottom: 15px;
      border-radius: 8px;
      border: none;
      background: #333;
      color: #fff;
    }

    .order-form button {
      background: #f4b400;
      border: none;
      padding: 12px 20px;
      font-size: 16px;
      font-weight: bold;
      color: #111;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .order-form button:hover {
      background: #d99a00;
    }
    
/* ---------------- FOOTER ---------------- */
footer {
  padding: 30px 30px;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  max-width: 1300px;
  justify-content: center;
  align-items: center;
  margin: auto;
  margin-top: 200px;
  
}

footer a {
  color: var(--text);
  text-decoration: none;
  font-size: 14px;
}

footer a:hover {
  color: var(--primary);
}

.footer-grid {
  display: flex;
  flex-wrap: wrap;
  
  gap: 40px;
  margin-top: 20px;

}

footer .muted {
  color: var(--muted);
  font-size: 14px;
}

    @media (max-width: 768px) {

      header{
        padding: 22px 30px ;
      }
       .hamburger{
  
    margin-top: 25px;
}
      .container {
        grid-template-columns: 1fr;
      }
  
  nav {
    display: none;
  }
  .hamburger {
    display: flex;
  }
    }
  </style>
</head>
<body>
  <header>
      <div class="brand">
        <a class="logo" href="/">
          <div class="title">Velora</div>
         
          <div class="subtitle">By HK</div>
        </a>
      </div>


      <div class="hamburger" id="hamburger" aria-label="Menu" aria-expanded="false" role="button">
        <span></span>
        <span></span>
        <span></span>
      </div>

      <nav id="navDesktop">

        <a href="#products">Products</a>
        <a href="#articles">Journal</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="/record">Orders</a>

        <button class="cta" id="shopNow">Shop Now</button>
      </nav>

      <div class="mobile-nav" id="mobileNav" aria-hidden="true">
        <button class="close-btn" aria-label="Close menu">✕</button>
        <a href="#products">Products</a>
        <a href="#articles">Journal</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="/record">Orders</a>
        <button class="cta" id="shopNowMobile">Shop Now</button>
        <div style="margin-top:12px;display:flex;gap:10px;align-items:center;justify-content:space-between">

    </header>


  <div class="container">
    <!-- Product Image -->
    <div class="product-img">
      <img src="<%= order.image %>" alt="Velora">
    </div>

    <!-- Product Info -->
    <div class="product-info">
      <h1><%= order.title %></h1>
      <p><%= order.details %><p><strong>Fragrance Notes:</strong><%= order.notes %></p>
      <p class="price">PKR : <%= order.price %></p>

      <!-- Order Form -->
      <div class="order-form">
        <h2>Place Your Order</h2>
        <form action="/order/<%=order._id%>" method="POST" enctype="multipart/form-data">
          <input type="text" name="name" placeholder="Your Name" required>
          <input type="email" name="email" placeholder="Your Email" required>
          <input type="text" name="phone" placeholder="Phone Number" required>
          <input type="text" name="address" placeholder="Delivery Address" required>
          <select name="quantity" id="quantity" required>
            <option value="">Select Quantity</option>
            <option value= 1>1</option>
            <option value= 2>2</option>
            <option value= 3>3</option>
            <option value= 4>4</option>
            <option value= 5>5</option>
          </select>
          <h2>Complete Your Payment</h2>

          <br>
          


          <div id="totalAmount"></div>

          <br>

          
         
          
          <select id="method" name="method" required onchange="showDetails(this.value)">
        <option value="">-- Select Payment Method --</option>
        <option value="easypaisa">Easypaisa</option>
        <option value="bank">Bank Transfer</option>
      </select>

       <div id="paymentDetails" class="account-info" style="display:none;"></div>
       <br>
       <br>

 <div>Upload Payment Proof</div>
 <br>
      <input type="file" id="proof" name="proof" accept="image/*,.pdf" required>


          <button type="submit">Confirm Order</button>
        </form>
      </div>
    </div>
  </div>

    <footer>
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:12px;flex-wrap:wrap">
        <div>
          <div style="font-weight:700">Velora</div>
          <div class="muted" style="max-width:30ch">By HK — Fragrance Redefined. Handcrafted scents, responsibly
            sourced.</div>
        </div>

        <div class="footer-grid">

           <div style="min-width:160px">
            <div style="font-weight:700">Media</div>
              <a href = "https://www.instagram.com/velorabyhk?utm_source=ig_web_button_share_sheet&igsh=ZDNlZDc0MzIxNw=="><i class="fab fa-instagram"></i> instagram</a>
              <br>
              <a href="https://wa.me/923107175839" target="_blank"><i class="fab fa-whatsapp"></i> whatsapp</a>
</a>
            <div class="muted"></div>
          </div>
 


          <div style="min-width:160px">
            <div style="font-weight:700">Shop</div>
            <div class="muted">Products • Limited editions • Gift sets</div>
          </div>

          <div style="min-width:160px">
            <div style="font-weight:700">Support</div>
            <div class="muted">Shipping • Returns • Contact</div>
          </div>
        </div>
      </div>

      <div
        style="margin-top:18px;display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap">
        <div class="muted small">© 2025 Velora by HK — All rights reserved.</div>
        <div class="muted small">Made with care • <a href="/privacy">Privacy</a> • <a href="/terms">Terms</a> </div>
      </div>
    </footer>

</body>



  <script>
  function showDetails(value) {
    const details = document.getElementById("paymentDetails");
    if (value === "easypaisa") {
      details.style.display = "block";
      details.innerHTML =
        "<b>Easypaisa Number:</b> 03275405747<br><b>Account Name:</b> Huzaifa khalid";
    } else if (value === "bank") {
      details.style.display = "block";
      details.innerHTML =
        "<b>Bank:</b> Meezan Bank<br><b>Account No:</b> 00300111258935<br><b>Account Title:</b> Huzaifa khalid";
    } else {
      details.style.display = "none";
    }
  }

  window.onload = function () {
    const price =Number("<%= order.price %>"); // product price from server
    const quantitySelect = document.getElementById("quantity");
    const totalAmountDiv = document.getElementById("totalAmount");

    if (quantitySelect) {
      quantitySelect.addEventListener("change", () => {
        const qty = parseInt(quantitySelect.value);
        if (!isNaN(qty)) {
          const total = qty * price;
          totalAmountDiv.innerText = `Total Amount: PKR ${total}`;
        } else {
          totalAmountDiv.innerText = "";
        }
      });
    }
  };

  
 
  
    // Simple product data for modal lookup

    const hamburger = document.querySelector('#hamburger');
    const mobileNav = document.querySelector('#mobileNav');
    const navDesktop = document.querySelector('.navDesktop');
    const closeBtn = document.querySelector('.close-btn');

    // SAFETY checks

    hamburger.addEventListener('click', () => {

      
      const expanded = hamburger.getAttribute('aria-expanded') === 'true';
      hamburger.setAttribute('aria-expanded', !expanded);
      mobileNav.classList.toggle('active');
      mobileNav.style.display="flex";

    });


    function closeMenu() {
      mobileNav.classList.remove('active');
      mobileNav.style.display="none";
      mobileNav.setAttribute('aria-hidden', 'true');
      hamburger.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = ''; // restore scroll
    }




    closeBtn.addEventListener('click', closeMenu);

  





    // contact form handling (demo only)


    // close modal with escape and backdrop click
  
  


</script>


</html>
